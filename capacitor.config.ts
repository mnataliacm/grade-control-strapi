import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'grade-control-strapi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
